name: Caller workflow
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "environment"
        type: environment
        required: true
jobs:
  init:
    runs-on: ubuntu-latest
    name: "my test"
    steps:
     - name: EnvVar
       run: |
        echo "${{ vars.SIZE_VAR }}" > data.json
        name=$(cat data.json | jq -r '.Dev')






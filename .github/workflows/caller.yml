name: Caller workflow
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "environment"
        type: environment
        required: true
jobs:
  init:
    runs-on: ubuntu-latest
    name: "my test"
    steps:
     - name: Set up environment variables
       run: |
          env_data_json="${{ toJson(vars.SIZE_VAR) }}"
          echo "Rele : $env_data_json"
          KEY1_VAR=$(echo "${{ toJson(vars.SIZE_VAR) }}" | jq -r '.Dev')
          echo "C : $KEY1_VAR"



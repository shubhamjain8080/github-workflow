name: Caller workflow
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "environment"
        type: environment
        required: true
jobs:
  init:
    runs-on: ubuntu-latest
    name: "my test"
    steps:
     - name: Set up environment variables
       run: |
          current_environment="${{ github.event.inputs.environment }}"
          echo "Current_Env : $current_environment"
          relevant_data=$(echo "${{ vars.SIZE_VAR }}" | jq -r '.Dev')
          

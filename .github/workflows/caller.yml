name: Caller workflow
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "environment"
        type: environment
        required: true
jobs:
  init:
    runs-on: ubuntu-latest
    name: "my test"
    steps:
     - name: EnvVar
       run: |
        jsonData=$(echo -e "${{ vars.SIZE_VAR }}" | jq -r '.Dev')





